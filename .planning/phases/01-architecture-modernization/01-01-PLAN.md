---
phase: 01-architecture-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/shared/schemas/username.ts
  - src/shared/schemas/index.ts
  - src/shared/hooks/.gitkeep
  - src/shared/utils/.gitkeep
  - src/utils/validators.ts
autonomous: true
requirements:
  - STRUCT-03
  - STRUCT-07

must_haves:
  truths:
    - "vitest coverage config uses glob patterns, not hardcoded file paths"
    - "Shared schemas directory exists at src/shared/schemas/ with username schema exported"
    - "src/shared/ directory structure is established (schemas, hooks, utils)"
    - "All existing tests still pass after changes"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Glob-based coverage config"
      contains: "src/features/**"
    - path: "src/shared/schemas/username.ts"
      provides: "Username Zod schema with exported max-length constant"
      exports: ["username", "USERNAME_MAX_LENGTH"]
    - path: "src/shared/schemas/index.ts"
      provides: "Barrel export for shared schemas"
  key_links:
    - from: "vitest.config.ts"
      to: "coverage include patterns"
      via: "glob patterns covering src/features/**, convex/**, src/shared/**"
      pattern: "src/features/\\*\\*"
---

<objective>
Fix vitest coverage config to use globs (so it survives the upcoming file restructure) and establish the src/shared/ directory with the first shared Zod schema (username). This is the foundation everything else builds on -- if coverage uses hardcoded paths, every subsequent plan that moves files will break the coverage threshold.

Purpose: Unblock all subsequent restructure plans by making coverage config path-agnostic, and create the shared infrastructure directory.
Output: Glob-based vitest config, src/shared/ directory with username schema.
</objective>

<execution_context>
@/Users/siraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/siraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architecture-modernization/01-RESEARCH.md

@vitest.config.ts
@src/utils/validators.ts
@errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert vitest coverage from hardcoded paths to globs</name>
  <files>vitest.config.ts</files>
  <action>
Replace the hardcoded `coverage.include` array with glob patterns that will survive file restructuring:

```
include: [
  "src/features/**/*.{ts,tsx}",
  "src/shared/**/*.{ts,tsx}",
  "src/ui/button.tsx",
  "src/ui/button-util.ts",
  "src/ui/input.tsx",
  "src/ui/switch.tsx",
  "src/ui/use-double-check.ts",
  "convex/**/*.ts",
  "errors.ts",
]
```

Keep the `exclude` array but update it to also use globs where sensible. Keep the existing excludes for auth, email, otp, generated files, test infrastructure, framework files, type-only files, Radix wrappers, route layouts, and login routes. Add `"src/routes/**"` to exclude since routes will become thin wrappers (not coverage-worthy). Keep existing UI component excludes.

IMPORTANT: Keep the 100% coverage thresholds. The glob patterns must cover exactly the files that currently have coverage -- no more, no less -- so existing tests still reach 100%. If the glob is too broad (pulls in uncovered files), use excludes to compensate.

Verify by running `npx vitest run --coverage` and confirming 100% thresholds still pass.
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && npx vitest run --coverage 2>&1 | tail -30</automated>
  </verify>
  <done>Coverage config uses globs, `npx vitest run --coverage` passes with 100% thresholds, no hardcoded file paths remain in coverage.include</done>
</task>

<task type="auto">
  <name>Task 2: Create src/shared/ directory structure with username schema</name>
  <files>src/shared/schemas/username.ts, src/shared/schemas/index.ts, src/shared/hooks/.gitkeep, src/shared/utils/.gitkeep</files>
  <action>
1. Create `src/shared/schemas/username.ts`:
   - Move the username Zod validator from `src/utils/validators.ts`
   - Export `USERNAME_MAX_LENGTH = 20` as a named constant
   - Export the `username` schema that uses this constant
   - Keep the same validation logic: `z.string().min(3).max(USERNAME_MAX_LENGTH).toLowerCase().trim().regex(/^[a-zA-Z0-9]+$/, "Username may only contain alphanumeric characters.")`

2. Create `src/shared/schemas/index.ts`:
   - Barrel export: `export { username, USERNAME_MAX_LENGTH } from "./username";`

3. Create placeholder directories:
   - `src/shared/hooks/.gitkeep`
   - `src/shared/utils/.gitkeep`

4. Update `src/utils/validators.ts` to re-export from the new location for backward compatibility:
   - `export { username } from "@/shared/schemas/username";`
   - This keeps existing imports working until Plan 01-02/01-03 update them directly

5. Make sure the new shared schema files are NOT excluded from coverage in vitest.config.ts (the glob `src/shared/**/*.{ts,tsx}` from Task 1 should cover them, but verify there's no conflicting exclude).

Do NOT update any consumers yet (route files, convex files) -- that happens in Plans 01-02 and 01-03.
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && npx vitest run --coverage 2>&1 | tail -30</automated>
  </verify>
  <done>src/shared/ directory exists with schemas/username.ts exporting USERNAME_MAX_LENGTH and username schema. src/utils/validators.ts re-exports from new location. All tests pass with 100% coverage.</done>
</task>

</tasks>

<verification>
- `npx vitest run --coverage` passes with 100% thresholds
- `npm run typecheck` passes
- `src/shared/schemas/username.ts` exists and exports USERNAME_MAX_LENGTH + username
- vitest.config.ts coverage.include has NO hardcoded individual file paths (except UI components that are specific includes)
</verification>

<success_criteria>
- vitest coverage uses glob patterns that will survive file restructuring
- src/shared/ directory structure is established
- Username schema lives in src/shared/schemas/ with exported constant
- Backward compatibility maintained via re-export in src/utils/validators.ts
- 100% coverage and typecheck pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-modernization/01-01-SUMMARY.md`
</output>
