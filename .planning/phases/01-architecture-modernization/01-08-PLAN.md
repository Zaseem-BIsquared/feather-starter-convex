---
phase: 01-architecture-modernization
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/_app/_auth/dashboard/_layout.checkout.test.tsx
  - src/routes/_app/_auth/dashboard/_layout.settings.billing.test.tsx
  - src/routes/_app/_auth/dashboard/_layout.settings.index.test.tsx
autonomous: true
requirements: [STRUCT-08]
gap_closure: true

must_haves:
  truths:
    - "npm run typecheck passes with zero errors"
  artifacts:
    - path: "src/routes/_app/_auth/dashboard/_layout.checkout.test.tsx"
      provides: "Checkout route tests without TS errors"
      contains: "import.*vi.*from.*vitest"
    - path: "src/routes/_app/_auth/dashboard/_layout.settings.billing.test.tsx"
      provides: "Billing settings route tests without TS errors"
      contains: "as HTMLElement"
    - path: "src/routes/_app/_auth/dashboard/_layout.settings.index.test.tsx"
      provides: "Settings route tests without TS errors"
  key_links: []
---

<objective>
Fix 8 TypeScript errors in 3 route test files so `npm run typecheck` passes cleanly.

Purpose: The success criterion "npm run typecheck and npm test pass" is not met due to 8 typecheck errors in route test files (missing `vi` import, `.focus()` on Element instead of HTMLElement, unused variables).
Output: All 3 test files compile without errors; `npm run typecheck` exits 0.
</objective>

<execution_context>
@/Users/siraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/siraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architecture-modernization/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix typecheck errors in route test files</name>
  <files>
    src/routes/_app/_auth/dashboard/_layout.checkout.test.tsx
    src/routes/_app/_auth/dashboard/_layout.settings.billing.test.tsx
    src/routes/_app/_auth/dashboard/_layout.settings.index.test.tsx
  </files>
  <action>
Fix 8 TypeScript errors across 3 files:

**_layout.checkout.test.tsx (3 errors: TS2304 Cannot find name 'vi'):**
- Add `vi` to the existing vitest import: change `import { describe, expect, it } from "vitest"` to `import { describe, expect, it, vi } from "vitest"` (note: this file uses `vi.useFakeTimers`, `vi.advanceTimersByTimeAsync`, `vi.useRealTimers` but never imported `vi`).

**_layout.settings.billing.test.tsx (4 errors: TS2339 focus on Element x2, TS6133 unused vars x2):**
- Line 196 (`proCard.focus()`): Cast `.closest('[role="button"]')!` to `HTMLElement` before calling `.focus()`: `const proCard = screen.getByText("Pro plan").closest('[role="button"]') as HTMLElement;` then `proCard.focus();`
- Line 364 (same pattern): Cast `.closest('[role="button"]')!` to `HTMLElement`.
- Line 210 (`proPlanId` unused): Destructure only what's used. Change `const { freePlanId, proPlanId } = await seedPlans(testClient);` to `const { freePlanId } = await seedPlans(testClient);` since only `freePlanId` is used in the "clicking upgrade to pro" test (proPlanId is seeded but the test clicks Pro card by text, not by ID).
- Line 299 (`userId` unused): In the "shows Free badge when user has no subscription" test, the destructured `userId` is not used. Change `async ({ client, testClient, userId })` to `async ({ client, testClient })`.

**_layout.settings.index.test.tsx (1 error: TS6133 unused var):**
- Line 26 (`user` unused): In the "renders user info with username" test at line 26, `const user = await ctx.db.get(userId);` is assigned but never read. Remove this line -- it fetches the user only to immediately patch by `userId` on the next line. The `ctx.db.patch(userId, ...)` call on line 27 does not need the prior `get`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>npm run typecheck exits 0 with zero errors. npm test still passes (160 tests).</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm test` still passes (all 160 tests green)
</verification>

<success_criteria>
- Zero TypeScript errors from `npm run typecheck`
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-modernization/01-08-SUMMARY.md`
</output>
