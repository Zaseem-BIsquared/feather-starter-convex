---
phase: 02.1
slug: stripe-plugin-extraction
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-10
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.x + convex-test 0.0.41 |
| **Config file** | vitest.config.ts |
| **Quick run command** | `npx vitest run` |
| **Full suite command** | `npx vitest run --coverage` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run`
- **After every plan wave:** Run `npx vitest run --coverage`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01-01 | 01 | 1 | Core runs without Stripe | unit + typecheck | `npx vitest run && npx tsc -p tsconfig.app.json --noEmit` | ✅ | ⬜ pending |
| 01-02 | 01 | 1 | No billing imports remain | static | `npx tsc -p tsconfig.app.json --noEmit` | ✅ | ⬜ pending |
| 01-03 | 01 | 1 | Coverage stays at 100% | unit | `npx vitest run --coverage` | ✅ | ⬜ pending |
| 02-01 | 02 | 2 | E2E tests pass without billing | e2e | `npx playwright test` | ✅ | ⬜ pending |
| 02-02 | 02 | 2 | Plugin branches merge cleanly | manual + unit | `git merge plugin/<name> --no-commit && npx vitest run` | ✅ | ⬜ pending |
| 02-03 | 02 | 2 | dangerouslyIgnoreUnhandledErrors removable | unit | `npx vitest run --coverage` (without flag) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. Tests are being removed/moved, not created.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Plugin branches merge cleanly | Compatibility | Requires git merge simulation | `git checkout -b test-merge main && git merge plugin/<name> --no-commit` then `npx vitest run` |
| Post-extraction dashboard renders | UI correctness | Visual check | Run `npm run dev`, visit dashboard, confirm no billing UI |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
